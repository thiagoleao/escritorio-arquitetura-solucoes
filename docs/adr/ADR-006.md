# ADR-006 - Geração Padronizada de Artefatos por Atividade

**Status:** Proposta
**Data:** 15 de julho de 2026

## Contexto

Atividades de um planejamento de arquitetura frequentemente envolvem produzir um artefato concreto — um diagrama, uma ADR, um documento de integração (INT) — ou coletar insumos de descoberta, mapeamento e reuniões. Hoje nada disso é suportado pela aplicação: esses artefatos são produzidos inteiramente fora dela.

Uma restrição importante orienta este ADR: **não delegar a geração completa do documento/diagrama ao modelo de linguagem livremente**. Pedir a um LLM para "desenhar um diagrama C4" ou "escrever uma ADR" do zero, a cada execução, produz resultados com estrutura e nível de detalhe diferentes toda vez — inconsistente entre atividades, entre planejamentos e entre empresas. A aplicação deve, em vez disso, ter **templates estruturais fixos** por tipo de artefato, e usar o modelo de linguagem apenas para **preencher conteúdo dentro dessa estrutura** — nunca para decidir layout, seções ou estilo.

## Decisão

### 0. Classificação da atividade (manual nesta versão)

O modelo de dados atual só tem título/descrição livres — não há como saber, sem informação adicional, se uma atividade "envolve diagrama" ou "é descoberta". A atividade ganha um campo `activity_type` editável manualmente pelo usuário (mesmo padrão de campo editável já usado para classificação de feedback), e é esse campo que decide quais botões aparecem no card (diagrama / ADR-INT / upload de insumo).

**Débito técnico registrado:** a classificação deveria, no futuro, ocorrer de forma automática (o modelo de linguagem sugere o `activity_type` ao gerar o planejamento, e o usuário só corrige quando necessário — mesmo padrão já cogitado para `solution_type`/domínio, também adiado desde a Fase 3 do ADR-002 por falta de volume de dados). Não implementado agora porque adicionaria mais uma inferência ao fluxo de geração sem volume de uso real que justifique o esforço de validação. Revisitar quando houver evidência de que a classificação manual é um atrito real no uso do dia a dia.

### 1. Geração de diagramas

Atividades classificadas como "envolve diagrama" ganham um botão "Gerar diagrama" no card. Modelos suportados inicialmente:
- Diagrama de fluxo;
- C4 Model, nos 4 níveis: Contexto, Container, Componente e Código.

Cada tipo de diagrama tem um template estrutural fixo (posicionamento, estilos, formas pré-definidas) em XML compatível com draw.io. O modelo de linguagem recebe esse template e só preenche o conteúdo variável (nós, rótulos, relações, textos) — nunca gera o XML do zero nem decide layout. Saída: arquivo `.drawio`, aberto diretamente no draw.io sem necessidade de ajuste manual de formatação.

### 2. Geração de ADR / documento de integração (INT)

Atividades classificadas como "envolve ADR" ou "envolve documento de integração" ganham um botão de geração equivalente. Mesmo princípio do item 1: um template de documento fixo (seções, ordem, formatação) onde o modelo preenche o conteúdo de cada seção. Formato de saída (`.docx` ou markdown) e ferramenta de geração a definir na fase de design técnico desta ADR.

### 3. Identidade visual por empresa

Os dois geradores (diagrama e documento) permitem trocar **apenas o logotipo** (e possivelmente uma cor de destaque) por empresa, sem alterar a estrutura do template. Implica associar um asset de logotipo a cada `company` já existente no modelo de dados.

### 4. Upload de insumos por atividade

Atividades classificadas como "descoberta", "mapeamento" ou "reunião" ganham um botão de upload de arquivo no card, associando o(s) arquivo(s) àquela atividade específica — diferente do upload já existente no ADR-001, que é sobre o planejamento como um todo, não sobre uma atividade individual.

## Riscos e considerações

- **Esforço de design técnico dos templates C4 é o maior risco de escopo desta ADR.** Desenhar layouts compatíveis com draw.io que fiquem visualmente corretos nos 4 níveis do C4 Model é trabalho de design técnico não trivial, provavelmente maior que a integração do LLM em si.
- **Reaproveitamento de ferramentas existentes.** A geração de documentos Word pode reaproveitar o skill de geração de `.docx` já disponível no ambiente de desenvolvimento, reduzindo o esforço da geração de ADR/INT.
- **Onde o novo relacionamento `activity -> attachment` vive.** Diferente do upload do ADR-001 (por requisição, descartado), este upload precisa persistir associado à atividade — implica nova tabela no `planner-api`, análoga a `activity_feedback` mas para arquivos.

## Fora de escopo

Edição visual dos diagramas dentro da própria aplicação (a edição continua no draw.io, fora da aplicação); templates de diagrama além de fluxo e C4 (ex.: diagrama de sequência, modelo entidade-relacionamento) — podem ser adicionados depois seguindo o mesmo padrão de template fixo; OCR de arquivos enviados (já fora de escopo desde o ADR-001); geração automática de todo o conteúdo de um artefato sem revisão humana — os artefatos gerados continuam sendo rascunhos a revisar, mesmo com estrutura padronizada.

## Critérios de sucesso

- Uma atividade marcada como "tipo diagrama" mostra o botão de geração correspondente e produz um `.drawio` que abre sem erros e visualmente consistente no draw.io.
- Uma atividade marcada como "tipo ADR" ou "tipo INT" mostra o botão de geração correspondente e produz um documento com estrutura fixa e conteúdo preenchido pelo modelo.
- Trocar o logotipo cadastrado de uma empresa muda a identidade visual de todos os artefatos gerados para ela, sem exigir alteração no template.
- Uma atividade de descoberta/mapeamento/reunião permite anexar um ou mais arquivos, e o conteúdo extraído fica associado a ela (consultável depois).
